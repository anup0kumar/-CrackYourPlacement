class Solution {
public:
    string convertToTitle(int columnNumber) {
      string ans;
      while(columnNumber){
        columnNumber--;
        int remain = columnNumber%26;
        char ch = remain + 'A';
        ans.push(ch);
        remain /=26;
    }
  reverse(ans.begin(),ans.end());
  return s;
}
};
